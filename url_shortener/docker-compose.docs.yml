services:
  docs:
    image: url_shortener_local_docs
    container_name: url_shortener_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - path: ./.envs/.local/.django
        required: false
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./url_shortener:/app/url_shortener:z
    ports:
      - '9000:9000'
    command: /start-docs
